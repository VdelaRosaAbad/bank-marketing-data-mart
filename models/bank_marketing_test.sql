-- Modelo de prueba para Bank Marketing
SELECT 
    25 as age,
    'admin.' as job,
    'married' as marital,
    'university.degree' as education,
    'no' as subscription,
    CURRENT_TIMESTAMP() as created_at
LIMIT 1 